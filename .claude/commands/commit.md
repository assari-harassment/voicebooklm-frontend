---
description: 変更内容から日本語コミットメッセージを生成してコミット
---

以下のコマンドで現在の変更を確認してください：

```bash
git status
git diff --staged
git diff
```

確認した変更内容をもとに、以下のルールでコミットしてください：

## ルール
- 日本語で一文、簡潔に書く
- 改行しない
- 何を変更したかが分かるように書く

## 手順
1. 必要なファイルを `git add` する
2. コミットメッセージを提案して確認を取る
3. `git commit -m "メッセージ"` でコミット
